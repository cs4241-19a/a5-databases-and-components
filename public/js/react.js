import ReactDOM from 'react-dom'

function sessionTimer(){
  const element = (<div><p>Current Time: {new Date().toLocalTimeString()}</p></div>)
  ReactDOM.render(element. document.getElementById('root'));
  }